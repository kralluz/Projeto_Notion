import { HeaderStyle } from "./style";

export const Header = ( ) => {
    return (
        <>
            <HeaderStyle>
                <span>Página</span>
            </HeaderStyle>
        </>
    );
};
